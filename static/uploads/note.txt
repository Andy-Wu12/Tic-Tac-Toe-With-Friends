This folder can't be empty or won't be tracked by git. This file is just used to make sure that doesn't happen.
The uploads folder will be where user-provided images (for profile pictures) will be stored.
Check getProfile(), updateProfile(), and getImage() for the GET and POST process.